const form=document.querySelector('#analysisLoginForm'),message=document.querySelector('#analysisLoginMessage'),password=document.querySelector('#analysisPassword');document.querySelector('#analysisToggle').addEventListener('click',()=>{password.type=password.type==='password'?'text':'password'});form.addEventListener('submit',async event=>{event.preventDefault();message.textContent='';const button=form.querySelector('.submit');button.disabled=true;try{const values=Object.fromEntries(new FormData(form));const response=await fetch('/api/analysis/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(values)});const data=await response.json();if(!response.ok)throw new Error(data.error||'ورود ناموفق بود.');location.replace('/analysis')}catch(error){message.textContent=error.message;button.disabled=false}});
